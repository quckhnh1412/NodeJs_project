<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User Profile</title>
    <style>
    #container {
            font-family: 'Arial', sans-serif;
            background-color: #f4f4f4;
            margin: 0;
            display: flex;
            align-items: center;
            justify-content: center;
            height: 100vh;
        }

        #user-information {
            background-color: #fff;
            padding: 30px;
            border-radius: 8px;
            box-shadow: 0 0 20px rgba(0, 0, 0, 0.1);
            width: 300px;
            text-align: center;
        }

        img {
            max-width: 200px;
            height: 200px;
            margin-top: 10px;
            border-radius: 50%;
        }

        label {
            display: block;
            margin: 10px 0 5px;
            color: #555;
            font-weight: bold;
        }

        input {
            width: calc(100% - 22px);
            padding: 8px;
            margin: 3px 0 15px;
            box-sizing: border-box;
            border: 1px solid #ccc;
            border-radius: 4px;
        }

        .button-container {
            display: flex;
            justify-content: space-between;
            margin-top: 10px;
        }

        button {
            background-color: #4caf50;
            color: #fff;
            padding: 12px;
            border: none;
            border-radius: 4px;
            cursor: pointer;
            width: 48%; /* Adjust the width as needed */
        }

        button:hover {
            background-color: #942b94;
        }
    </style>
</head>

<body>
    <div id="container">  
        <form id="user-infomation"  action="/admin/update-profile" method="POST" enctype="multipart/form-data">
        <h1>User Profile</h1>

        <div>
            {{#if user.imageUrl}}
             <img src="images/avatar/{{user.imageUrl}}" alt="User Image">
    
            {{/if}}
            <label for="image">Upload Image:</label>
            <input type="file" id="image" name="image" accept="image/*" required disabled>

            <label for="name">Name:</label>
            <input type="text" id="name" name="name" value="{{user.name}}" disabled>

            <label for="email">Email:</label>
            <input type="email" id="email" name="email" value="{{user.email}}" disabled>

            <label for="username">Username:</label>
            <input type="text" id="username" name="username" value="{{user.username}}" disabled>

            <div class="button-container">
                    <button type="button" style=" background-color: #e788e8;" onclick="enableEdit()">Edit</button>
                    <button type="submit"  style="display:none;  background-color: #c30ec6;">Save</button>
                </div>
        </div>
    </form>
</div>
  
    <script>
        function enableEdit() {
            const input = document.getElementById("image");
   
            input.removeAttribute('disabled');
       

            const saveButton = document.querySelector('button[type="submit"]');
            saveButton.style.display = 'block';
        }
    </script>
</body>

</html>
